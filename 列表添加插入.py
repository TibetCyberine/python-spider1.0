a= [1,2,3]
#在列表末尾添加元素0
a.append(0)
#在列表中插入一个数8，放在第一位
a.insert(8,0)
#列表中去掉第一个出现与设定相匹配的的元素
#这里设定成2
a.remove(2)
print(a[0])#打印列表里第一个元素
print(a[-1])#打印列表最后一个元素，负数表示从后往前数
a.index(2)#第一次出现传入值的位置
a.count(2)#出现传入值的次数
a.sort()#从小到大排序
a.sort(reverse = True)#从大到小排序
